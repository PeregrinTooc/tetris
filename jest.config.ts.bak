export default {
    transform: {},
    moduleNameMapper: {
        "^(\\.{1,2}/.*)\\.ts$": "$1",
        "^(\\.{1,2}/.*)\\.mjs$": "$1"
    },
    testEnvironment: "jsdom",
    testMatch: [
        "**/__tests__/**/*.ts",
        "**/?(*.)+(spec|test).ts"
    ],
    setupFilesAfterEnv: ["./jest.setup.ts"]
}
